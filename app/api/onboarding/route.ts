import { createCipheriv,createHash,randomBytes } from 'node:crypto';
import { NextRequest,NextResponse } from 'next/server';
import { z } from 'zod';
import { createServiceRoleClient,createUserScopedClient } from '@/lib/supabase/server';
const schema=z.object({fullName:z.string().min(2),dateOfBirth:z.string().date(),phone:z.string().min(7),personalEmail:z.string().email(),address:z.record(z.string(),z.string()),emergencyContact:z.object({name:z.string().min(2),relation:z.string().min(1),phone:z.string().min(7)}),accountHolderName:z.string().min(2),bankName:z.string().min(2),accountNumber:z.string().regex(/^\d{6,20}$/),confirmAccountNumber:z.string(),ifscCode:z.string().regex(/^[A-Z]{4}0[A-Z0-9]{6}$/),pan:z.string().regex(/^[A-Z]{5}[0-9]{4}[A-Z]$/),uan:z.string().regex(/^\d{12}$/).optional().or(z.literal('')),profilePhotoPath:z.string().max(500).optional()}).refine(v=>v.accountNumber===v.confirmAccountNumber,{path:['confirmAccountNumber'],message:'Bank account numbers do not match'});
function encrypt(value:string){const configured=process.env.BANK_DATA_ENCRYPTION_KEY;if(!configured)throw new Error('BANK_DATA_ENCRYPTION_KEY is not configured');const key=createHash('sha256').update(configured).digest();const iv=randomBytes(12);const cipher=createCipheriv('aes-256-gcm',key,iv);const ciphertext=Buffer.concat([cipher.update(value,'utf8'),cipher.final()]);return `v1:${iv.toString('base64')}:${cipher.getAuthTag().toString('base64')}:${ciphertext.toString('base64')}`}
export async function POST(request:NextRequest){try{const token=request.headers.get('authorization')?.replace(/^Bearer\s+/,'');if(!token)return NextResponse.json({error:{code:'UNAUTHENTICATED',message:'Bearer token required'}},{status:401});const input=schema.parse(await request.json());const scoped=createUserScopedClient(token);const service=createServiceRoleClient();const {data:auth}=await scoped.auth.getUser(token);if(!auth.user)return NextResponse.json({error:{code:'UNAUTHENTICATED',message:'Invalid session'}},{status:401});const {data:employee}=await scoped.from('employees').select('*').eq('user_id',auth.user.id).single();if(!employee)throw new Error('No employee record is linked to this account');await service.from('profiles').update({full_name:input.fullName,date_of_birth:input.dateOfBirth,phone:input.phone,personal_email:input.personalEmail,address:input.address,emergency_contact:input.emergencyContact}).eq('id',auth.user.id);const bank=await service.from('employee_bank_accounts').insert({company_id:employee.company_id,employee_id:employee.id,account_holder_name:input.accountHolderName,bank_name:input.bankName,account_number_encrypted:encrypt(input.accountNumber),account_last4:input.accountNumber.slice(-4),ifsc_code:input.ifscCode,is_primary:true,verification_status:'submitted',is_verified:false});if(bank.error)throw bank.error;await service.from('employees').update({full_name:input.fullName,phone:input.phone,onboarding_status:'pending_verification',onboarding_completed_at:new Date().toISOString(),pan_encrypted:encrypt(input.pan),uan_encrypted:input.uan?encrypt(input.uan):null,profile_photo_path:input.profilePhotoPath??employee.profile_photo_path}).eq('id',employee.id);await service.from('audit_logs').insert({company_id:employee.company_id,actor_user_id:auth.user.id,action:'employee_onboarding_submitted',entity_table:'employees',entity_id:employee.id,summary:{bank_last4:input.accountNumber.slice(-4)}});return NextResponse.json({status:'pending_verification',message:'Onboarding submitted for HR verification'});}catch(error){return NextResponse.json({error:{code:'ONBOARDING_ERROR',message:error instanceof Error?error.message:'Unable to submit onboarding'}},{status:400})}}
